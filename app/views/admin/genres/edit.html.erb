
<h2>ジャンル編集ページ</h2>

<%= form_for(@genre, {:url => admin_genre_path}) do |f| %>
	<div class="filed row">
		<%= f.text_field :name %>
	</div>
	<div class="actions row">
		<%= f.submit %>
	</div>

<% end %>