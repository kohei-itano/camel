<div class="col-xs-5 all">
	<h2>User List</h2>
	<% @users.each do |user| %>
		<div class="users">
			<div class="image">
				<%= attachment_image_tag user, :profile_image, :fill, 300, 200%>
			</div>
				<table class="table">
					<tr>
						<th>name</th>
						<td>
							<%= link_to user_path(user.id) do %>
								<%= user.name %>
							<% end %>
						</td>
					</tr>
					<tr>
						<th> introduction</th>
						<td width="300"><%= user.introduction %></td>
					</tr>
				</table>
		</div>
	<% end %>
	<div class="paginate">
		<td><%= paginate @users, class: "paginate" %></td>
	</div>
</div>